import React from 'react'
import { LoaderWrapper, LoaderIcon } from './Loader.styles';

export const Loader = () => {
    return (
        <LoaderWrapper>
            <LoaderIcon />
        </LoaderWrapper>
    )
}

export default Loader;
